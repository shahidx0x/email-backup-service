apiVersion: 1
providers:
  - name: Email Backup
    orgId: 1
    folder: Email Backup
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

const base=process.env.NEXT_PUBLIC_API_BASE_URL??'/api';
export function accessToken(){return typeof window==='undefined'?null:sessionStorage.getItem('access_token')}
export async function api<T>(path:string,init:RequestInit={}):Promise<T>{const token=accessToken();const response=await fetch(`${base}${path}`,{...init,headers:{'content-type':'application/json',...(token?{authorization:`Bearer ${token}`}:{ }),...init.headers},credentials:'include'});if(response.status===401&&typeof window!=='undefined'){sessionStorage.removeItem('access_token');location.href='/login';throw new Error('Authentication required')}if(!response.ok){const body=await response.json().catch(()=>({message:'Request failed'})) as {message?:string};throw new Error(body.message??'Request failed')}return response.json() as Promise<T>}
